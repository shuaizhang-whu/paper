# Paper Management System / 论文管理系统

一个简单的论文上传和记录管理系统。

## 功能特性

- 📤 **上传论文**: 支持上传各种格式的论文文件（PDF, DOC, DOCX等）
- 📝 **记录元数据**: 记录论文的标题、作者、年份、会议/期刊等信息
- 🔍 **搜索功能**: 支持按关键词搜索论文
- 🏷️ **标签管理**: 为论文添加标签便于分类
- 🔒 **防重复**: 通过MD5哈希自动检测重复上传
- 📊 **详细信息**: 查看论文的完整元数据

## 安装要求

- Python 3.6+
- 无需额外依赖（仅使用Python标准库）

## 快速开始

### 1. 克隆仓库

```bash
git clone https://github.com/shuaizhang-whu/paper.git
cd paper
```

### 2. 上传论文

```bash
# 基本用法
python paper_manager.py upload <论文文件路径> -t "论文标题"

# 完整示例
python paper_manager.py upload paper.pdf \
    -t "深度学习在计算机视觉中的应用" \
    -a "张三" "李四" "王五" \
    -y 2023 \
    -c "CVPR" \
    -n "这是一篇重要的综述论文" \
    --tags "深度学习" "计算机视觉" "综述"
```

### 3. 列出所有论文

```bash
# 列出所有论文
python paper_manager.py list

# 搜索论文
python paper_manager.py list -s "深度学习"

# 按标签过滤
python paper_manager.py list --tag "深度学习"
```

### 4. 查看论文详情

```bash
python paper_manager.py show <论文ID>
```

## 使用示例

### 上传论文

```bash
# 示例1: 上传会议论文
python paper_manager.py upload ResNet.pdf \
    -t "Deep Residual Learning for Image Recognition" \
    -a "Kaiming He" "Xiangyu Zhang" "Shaoqing Ren" "Jian Sun" \
    -y 2016 \
    -c "CVPR" \
    --tags "深度学习" "ResNet" "图像识别"

# 示例2: 上传期刊论文
python paper_manager.py upload transformer.pdf \
    -t "Attention Is All You Need" \
    -a "Ashish Vaswani" \
    -y 2017 \
    -c "NeurIPS" \
    --tags "Transformer" "NLP" "注意力机制"
```

### 列出和搜索论文

```bash
# 列出所有论文
python paper_manager.py list

# 搜索包含"深度学习"的论文
python paper_manager.py list -s "深度学习"

# 查找特定作者的论文
python paper_manager.py list -s "Kaiming He"

# 按标签筛选
python paper_manager.py list --tag "ResNet"
```

### 查看论文详细信息

```bash
# 查看ID为1的论文详情
python paper_manager.py show 1
```

## 目录结构

```
paper/
├── README.md              # 项目说明文档
├── paper_manager.py       # 论文管理主程序
├── papers/                # 存储上传的论文文件
│   └── .gitkeep
└── records/               # 存储论文元数据
    ├── .gitkeep
    └── papers.json        # 论文记录数据库（自动生成）
```

## 数据格式

论文记录保存在 `records/papers.json` 文件中，格式如下：

```json
[
  {
    "id": 1,
    "title": "论文标题",
    "authors": ["作者1", "作者2"],
    "year": 2023,
    "conference": "会议名称",
    "filename": "1_paper.pdf",
    "original_filename": "paper.pdf",
    "file_extension": ".pdf",
    "md5": "文件MD5哈希值",
    "upload_date": "2023-01-01T12:00:00",
    "notes": "备注信息",
    "tags": ["标签1", "标签2"]
  }
]
```

## 命令行参数说明

### upload 命令

| 参数 | 简写 | 必需 | 说明 |
|------|------|------|------|
| `file` | - | ✓ | 论文文件路径 |
| `--title` | `-t` | ✓ | 论文标题 |
| `--authors` | `-a` | ✗ | 作者列表（空格分隔） |
| `--year` | `-y` | ✗ | 发表年份 |
| `--conference` | `-c` | ✗ | 会议/期刊名称 |
| `--notes` | `-n` | ✗ | 备注信息 |
| `--tags` | - | ✗ | 标签列表（空格分隔） |

### list 命令

| 参数 | 简写 | 说明 |
|------|------|------|
| `--search` | `-s` | 搜索关键词 |
| `--tag` | - | 按标签过滤 |

### show 命令

| 参数 | 说明 |
|------|------|
| `id` | 论文ID |

## 特性说明

### 防重复上传

系统会计算每个上传文件的MD5哈希值，如果检测到相同的文件已经存在，会给出警告提示，避免重复上传。

### 文件命名

上传的论文文件会被重命名为 `<ID>_<原始文件名>` 的格式，便于管理和查找。

### 搜索功能

搜索功能支持在以下字段中查找：
- 论文标题
- 作者姓名
- 会议/期刊名称

搜索不区分大小写。

## 许可证

MIT License

## 贡献

欢迎提交Issue和Pull Request！